{
  "name": "animflow-dsl-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e:studio": "playwright test --config apps/web/playwright.config.ts",
    "verify": "pnpm test && pnpm build && pnpm lint",
    "verify:worker-bundle": "node scripts/check-worker-bundle.mjs",
    "benchmark:browser": "node scripts/run-browser-benchmark.mjs",
    "eval:skill": "node skills/animflow-authoring/evals/run-evals.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "esbuild": "0.28.2",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
